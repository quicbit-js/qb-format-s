{
  "name": "qb-format-s",
  "version": "1.0.4",
  "description": "A tiny implementation of printf spacing logic for strings",
  "repository": "quicbit-js/qb-format-s",
  "devDependencies": {
    "tape": "^4.6.3",
    "test-kit": "^2.1.3"
  },
  "scripts": {
    "test": "tap --coverage-report=html test.js",
    "dist": "mkdir -p dist && browserify index.js > dist/qb-format-s.js && cat dist/qb-format-s.js | uglifyjs -c -m > dist/qb-format-s.min.js"
  },
  "keywords": [
    "quicbit",
    "sprintf",
    "format",
    "printf",
    "small",
    "tiny"
  ],
  "author": "Matthew Voss",
  "license": "ISC"
}
